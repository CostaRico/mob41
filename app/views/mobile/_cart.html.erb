<ul class="navbar-mini-cart  nav">
  <!--<% unless order.nil? || order_count(order.item_count) == 0 %>-->
      <li class="nav-item dropdown">
        <a class="nav-link" href="/cart">
          <i class="fa fa-shopping-basket" style="margin-right: 0.3em;" aria-hidden="true"></i>
          <span class="cart-items-count count"><%= order.nil? ? 0 : order_count(order.item_count) %></span>
          <span class="cart-items-total-price total-price"><span class="amount"><%= order.nil? ? 0 : display_hum_price(order.total) %>
            руб.</span></span>
        </a>
      </li>
  <!--<% end %>-->
</ul>